import time
import numpy as np
import pickle
import awkward as ak
import dask_awkward as dak
from distributed import Client



def scoreEdgesBySigEff(events, load_path: str, save_path: str):
    """
    Unimplemented stub -- never wired up to any caller in this repo (see
    docs/known_issues.md, "Orphaned/pre-existing broken scripts"). Left as a
    placeholder rather than guessing the intended sub-category edge-finding
    algorithm.
    """
    raise NotImplementedError(
        "scoreEdgesBySigEff was never implemented and has no callers in this repo."
    )