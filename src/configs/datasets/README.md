This directory is for storing dataset paths in yaml file format for usage by run_prestage.py.
The era string (ie "2018") is both the name of the yaml file as well as the key to the yaml file contents for ease of dataset merging.
