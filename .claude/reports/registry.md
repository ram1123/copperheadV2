# CMS Analysis Work Registry

This file is a concise index of durable project work. It is not a conversation
log.

| Date | Type | Topic | Outcome | Report |
|---|---|---|---|---|
| 2026-08-31 | Investigation | Muon-selection implementation review (read-only) vs stored muon guidance | Open — 10 confirmed defects (cutflow/mask mismatch, loose trigger matching + hardcoded `filterBits & 8`, missing IP cuts, `is_run2` RERECO misclassification, dead correction config, chunk-biased 2017 trigger SF, `pt_raw` thresholds, MuonScaRe 26 GeV floor, `doing_BS_correction` decoupling, era-asymmetric systematics) + `[Verify]` list + `muons.md` gaps; no code changed | investigations/2026-08-31_muon-selection-review.md |
| 2026-08-31 | Investigation | Stage-1 runtime baseline + status tooling (v15, multi-scenario) | Open — per-sample cost from `_status/job_status.jsonl` (data ~300-410k evt/s, MC ~180-270k, Syst ~+30%); added `scripts/summarize_stage1_status.py` (done/failed/stuck + wall + runs-to-succeed as Markdown). Open failure clusters: `FilterEvents_Aug30/2024` (pre-fix JER/JEC, clearing on current run), `PUDNN_TrainOn2024_Syst/2024` (bitwise_and TypeError, commit f7b1d74a) | investigations/2026-08-31_stage1-runtime-baseline.md (+ _stage1-status-snapshot.md) |
| 2026-09-03 | Investigation | Stage-1 corrections/SF/reweighting inventory, Run 3 years 2024/2025/2026 (read-only) | Reference — pipeline-ordered inventory (§1.1-§1.27) with per-year switch/payload/tag/systematics + 3-year summary table + 24-item verification list. 2026 = "half scaffold": JME payloads real (`Summer24Prompt26_*`), MUO/LUM placeholder (reuse 2025/2024), MC = 2024-conditions Summer24. Key inconsistencies: `do_jet_horn_ptcut` 2024=50 vs 2025/2026=false (comment says parity); 2024 vs 2025/2026 `ecalBadCalibFilter` in `event_flags`; 2025 JEC unc labels reuse `*_2024` | investigations/2026-09-03_stage1-corrections-run3-2024-2026.md |
| 2026-09-03 | Investigation | Run 3 data eras + luminosity + MC dataset lists, 2024/2025/2026 (read-only) | Reference — per-era `lumi_pb` + datasets per year; analysis lumi 2024 109.82 / 2025 110.84 / 2026 25.95 fb⁻¹ (2026 preliminary, era C low-PU excluded). Findings: 2025 `total_lumi_pb` 110840 ≠ sum-of-eras 110850 (10 pb⁻¹); 2025 & 2026 MC are one shared 2024-conditions Summer24 set (2025 MC block == 2024's); `vbf_*` base+ext1 normalization to check; 2026 data uses Muon0-3 vs Muon0-1 | investigations/2026-09-03_run3-datasets-lumi-2024-2026.md |
| 2026-09-03 | Investigation | `plotter/plot_real_fake_jets_stack.py` review — does it work, how to run (read-only) | Reference — yes, real (`genJetIdx!=-1`)/fake stack plots, correct physics; but it's a stale duplicate of the actively-maintained/documented `MVA_training/pileup_symbolic_regression/plot_real_fake_jets_stack.py` (has region select, HE/HF cleaning, ratio pad — use that one instead). Most plotted vars need `do_add_jet_ID_vars: true`, which is `false` for every year in current `switches.yaml` (verified: only 4/38 vars present in CI sync 2017 DY MC); MC-only (crashes, doesn't skip, on data input) | investigations/2026-09-03_plot_real_fake_jets_stack_review.md |
| 2026-09-03 | Investigation | `jj_eta_region` control-plot "inconsistency" report — bug or physics? (read-only) | Closed — no bug: `applyRegionCatCuts`'s `jj_both_central`/`jj_non_central` masks verified as an exact partition against real data (26M-event `ttjets_dl` 2026 cross-check: central+non_central == njets>=2 exactly). Real cause is a genuine, monotonic Data/MC excess with jet forwardness (all 1.09 → both_central 0.94 → non_central 1.25 → both_he 1.73 → both_fwd30 1.58), matching the known Run3 HE-region JME mismodeling this project already tracks; seen on the loose-cut (`FilterEvents_Aug30_..._OfficialRecomendation`, no HE/HF mitigation) 2026 scenario. Follow-up not yet done: re-check with `--he-pt-cut`/`--hf-pt-cut` applied to confirm the ratio recovers | investigations/2026-09-03_jj-eta-region-data-mc-ratio.md |
| 2026-09-04 | Implementation | Split `Cat_ggh`/`Cat_nocat` jet-region plots by njets first (0j/1j/>=2j), per user physics spec | Done — `modules/selection.py`: new `SINGLE_JET_ETA_REGIONS` (njets==1-baked-in single-jet masks) alongside existing `PAIR_JJ_ETA_REGIONS`; 3 new `ValueError`s replacing silent-empty for invalid njets/region combos; `workflow/Snakefile`: `PLOT_TARGETS` now per-category (`vbf` unchanged pair matrix, now labeled njets_plot="2"; `ggh`/`nocat` get 0j/1j/2j targets). Bonus fix: `<`/`>` region boundaries at |eta|=2.5/3.0 were both-exclusive (uncovered exact-boundary events, confirmed non-negligible on data: 12+5 events/330k) — now half-open, exact partition. Verified via direct-data partition checks (ttjets_dl 2026) + clean `snakemake -n` DAG dry-run (46 jobs, only new 0j/1j targets + a few missing 2j ones need generating). Not done: no plots actually regenerated/inspected yet; njets==1 bucket's own Data/MC trend unexamined | implementations/2026-09-04_njets-first-jet-region-split.md |
| 2026-09-05 | Decision/fix | stage-1 crash for 2024 (`ConfigKeyError: Missing key 2024`, `switches.jer_strat.2024`) | Fixed — `configs/parameters/switches.yaml:366` had `"202-1": -1` (typo for `"2024"`) under `jer_strat`; only such typo in the file. Fixed key to `"2024"`, value unchanged (`-1`, matching surrounding Run3 years in this snapshot). Verified by re-running the user's exact command: passes config load, connects to their Dask Gateway cluster, begins processing `data_C` 2024. Full run left in progress in background at write time | decisions/2026-09-05_switches-yaml-jer-strat-2024-typo.md |
| 2026-09-09 | Implementation | Cutflow API review vs installed coffea 2026.5.0 + 2 real bugs found via `update_sync_references.sh` | Done — reordered `all_required_selections` to match actual `.add()` sequence (was producing misleading intermediate efficiencies); switched `cutflow_io.py` to public `cutflow.result()` (was reading private `_names`/`_nevcutflow`/`_nevonecut`); fixed `TotalEntries`/`.print()` nits. Weighted-cutflow attempt (`weights=weights`) **reverted** after a real run hit `IndexError` — `Weights(len(events),...)` is built *after* `events = events[event_filter==True]` reduces the population, so no length-matched weights array exists for `self.selection`'s (pre-reduction) masks; unweighted-only is the only valid option in this pipeline's architecture. Also fixed an unrelated pre-existing bug in `scripts/update_sync_references.sh`: hardcoded `cutflow_<sample>_0.json` name didn't match the real shard-based filename (`_build_shard_id`); ported the glob-based lookup already used correctly in `.github/workflows/sync-stage1.yml`. Verified: script run to completion twice (exit 0), regenerated `test/reference/{2017,2022preEE}_cutflow_*.json` + eventKinematics txt, inspected output directly (monotonic cumulative, correct order, no stray weighted keys) | implementations/2026-09-09_cutflow-api-review-and-fixes.md |
| 2026-09-09 | Investigation | Monitoring user-launched VBF DNN training, full Run3 2022preEE-2026, label `..._OfficialRecomendation` | Done, no errors — 2022-2023 stage1-data blocker resolved (all 7 years had compacted stage1 output). Confirmed 2 uncommitted local changes (not made by this session): `HPO_TRIALS` default 50->11, and `preprocess_dnn.py` hardcoding `jj_eta_region="jj_both_central"` (VBF DNN trained only on both-jets-\|eta\|<=2.5 events). HPO: 10/11 trials completed, trial 10 correctly pruned by `MedianPruner(n_startup_trials=10)` (not a bug — pruner only ever eligible for the last trial at this trial count); best val AUC 0.899 (trial 9). Final training all 4 folds clean: val/eval AUC ~0.893-0.905 (down from Aug23 run's 0.92-0.93, confounded by both the region restriction and fewer HPO trials — not disentangled); bkg KS 0.023-0.041, ~3-4x higher than Aug23's 0.006-0.012 (real, not alarming). User TODOs for after this run: make `jj_eta_region` configurable + reflect it in output dir name (separately: resolve `Week_June10` vs `main` conflicts) | investigations/2026-09-09_vbf-dnn-full-run3-monitoring.md |
| 2026-09-09 | Investigation | Full cross-check, years 2025/2026: luminosity, MC datasets/cross-sections, `switches.yaml` vs CMS POG recs (coordinated: physics-reviewer + main agent) | Open — 2 new regressions found beyond the 2026-09-03 report: `do_jet_horn_ptcut` now `false` for 2024-2026 despite its own comment/`jets.md` saying `50` (HE region has no forward-jet pT mitigation at all right now); 2025 `jec_unc_to_consider` still reuses `*_2024` source labels (latent, but live once `switches_profiles/syst.yaml` is applied for a 2025 systematics run — would break or silently mis-apply JES sources). Also: fixed earlier same session, `lumi_syst["2025"]` `KeyError` in `stage3/make_datacards.py` (added sourced 5%, uncorrelated, from `lumi.md`); 2026 deliberately has no `lumi_syst` entry (no official LUM value exists yet — do not invent one). Confirmed unchanged from Sept-3 report: 2025 lumi 10 pb⁻¹ era-sum mismatch, `vbf_*` base+ext1 normalization unverified (higher priority — affects signal yield), all 2025/2026 MUO/LUM/b-tag/PU-DNN/Z-pT placeholders. `switches.yaml` confirmed to be live session-state (profile-driven via `scripts/apply_switches_profile.py`, observed changing mid-session from another concurrent session) — re-verify before trusting any snapshot | investigations/2026-09-09_2025-2026-full-crosscheck.md |
| 2026-09-10 | Decision/fix | `run_stats_pipeline_VBF.sh -m 9` run, years 2022preEE-2026+Run3, label `..._OfficialRecomendation` | Done — full run succeeded (exit 0, real significance for every year including 2025/2026). Fixed 3 issues en route: (1) `usage()` docstring was stale vs the committed (ram1123, `4b57a44`) case-block renumbering — synced docstring + `CLAUDE.md` mode table to the real numbering, did not revert the renumbering; (2) first attempt failed (`Missing VBF SR/SB datacards`) from a date rollover — `-o` wasn't passed so it defaulted to today instead of the existing `Sep09_2026` stage3 output; reran with the matching postfix; (3) real bug: `collect_vbf_significance_summary`/`collect_vbf_limit_summary` (`common_workflow.sh`) had a hardcoded year list missing 2025/2026 — their significance logs existed on disk but were silently dropped from the summary CSV; fixed both functions, verified by regenerating the CSV. Known, unfixed, flagged-not-patched: `ensure_vbf_card Run3` still only combines 2022preEE-2024 (2025/2026 excluded from the Run3 combined row by pre-existing design) — an analysis-scope decision, not a bug | decisions/2026-09-10_stats-pipeline-mode9-run.md |
| 2026-09-10 | Implementation | Make VBF DNN `jj_eta_region` configurable + encode in output dir name (coordinated: code-reviewer + main agent) | Done, verified — `preprocess_dnn.py`: new `PreprocessConfig.jj_eta_region` (YAML `analysis.jj_eta_region`, `--jj-eta-region` CLI override), replaces the hardcoded `"jj_both_central"` in `applyRegionCatCuts`'s call; `make_output_dir()` appends `_{jj_eta_region}`; manifest/logs record it. Resolution order (after a follow-up fix): `JJ_ETA_REGION` env var > `analysis.jj_eta_region` in the DNN config YAML (the persistent source of truth, parsed by `common_workflow.sh`/`run_dnn.sh` via inline `python3 -c`) > `"all"` fallback. First cut of the wrapper unconditionally forced `--jj-eta-region all`, making YAML edits a no-op through `run_analysis_pipeline.sh` — user hit this (YAML `jj_non_central`, banner still `all`); fixed. `run_analysis_pipeline.sh`/YAML docs updated. Concurrent edit by someone else mid-task had set the *shipped* default to `"all"` (not `jj_both_central`) — kept, per instruction. Code-reviewer sub-agent found + fixed a real bug: `run_dnn.sh` (separate driver, not in original file list) computed its output path without the new suffix — would have mismatched `preprocess_dnn.py`'s now-suffixed dir; fixed to match. `hpo_optuna.py` checked directly (user asked) — no change needed, pure `--data-dir` pass-through, its `load_config` is `train_dnn.py`'s `TrainConfig` (never reads category/region/jj_eta_region). Verified via `bash -n`/`py_compile`/`--help`/direct resolution checks (not end-to-end real data). Practical consequence: through the wrapper, the effective region now = whatever `analysis.jj_eta_region` in the YAML says (or `JJ_ETA_REGION` if set); the just-completed full-Run3 run used `jj_both_central`. Noted: several touched files were `MM` (staged+modified) mid-task — someone `git add`ed a partial snapshot concurrently; nothing staged/committed by this session | implementations/2026-09-10_jj-eta-region-configurable.md |
| 2026-09-10 | Implementation | 2025 data yields via `scripts/get_yields.py` (label `..._OfficialRecomendation`, cutbased) + `--data-only`/`--data-glob` flags | Done — user's all-zero output was a wrong `--input`: bare ntuple root makes `resolve_input_layout()` fall to the "sample_root" branch and every per-process glob matches nothing (silent, no error). Fix: `--input <base>/stage1_output` -> resolves as `stage1` layout -> `.../2025/compacted`. Added opt-in `--data-only` (skip MC; faster + immune to the fragile 2025 MC set) and `--data-glob` (default `data*`, replaces the hardcoded data glob; `data_[C-G]` drops data_B). Both runs exit 0 (~4 min each). 2025 data all eras (raw=yield): nocat h-peak 788140 / h-sb 685323; ggh h-peak 725668 / h-sb 630030; vbf h-peak 8305 / h-sb 7222 (vbf all >=2j). Excluding data_B (~0.2%): nocat 786345 / 683717; ggh 724003 / 628540; vbf 8285 / 7210. h-peak = blinded 115-135 window. Outputs: `yield_10Sep_2025_cutbased_dataonly{,_noB}.csv` + `yield_10Sep_2025_summary{,_noB}.csv` (MC cols 0 by construction). Follow-up: compacted parquet is NOT mass-window filtered (full m_mm spectrum on disk), so added `scripts/count_dimuon_mass_window.py` (pyarrow column-only, no Dask) + `docs/GetYields.md` section for plain mass-window counts with no category/region cut. 2025 data C-G: 70<m<110 = 75,960,754; 76<m<106 = 74,092,153 (data_B +179,468 / +175,085). `[115,135)`=786,345 and `[110,115)+[135,150)`=683,717 reproduce get_yields nocat h-peak/h-sb exactly | implementations/2026-09-10_get-yields-2025-data.md |